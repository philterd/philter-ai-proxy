services:

  philter-ai-proxy:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./config.example.yaml:/app/config.yaml:ro
    environment:
      - PHILTER_PROXY_CONFIG=/app/config.yaml
